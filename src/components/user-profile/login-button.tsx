import { Button } from "../ui/button";
import LoginDialog from "./login-dialog";

export default function LoginButton() {
  return (
    <LoginDialog>
      <Button>Login</Button>
    </LoginDialog>
  );
}
